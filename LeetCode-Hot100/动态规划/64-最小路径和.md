---
type: solution
project: leetcode-hot100
problem: 64
title: "64. 最小路径和"
difficulty: 中等
tags:
  - LeetCode
  - DP
  - 二维DP
  - 网格路径
created: 2026-05-11
updated: 2026-05-11
---

# 64. 最小路径和

> 链接：https://leetcode.cn/problems/minimum-path-sum/

## 题目描述

`m × n` 网格，每格有非负整数。从左上角到右下角，每次只能向右或向下，找一条路径使路径上的数字总和最小。

### 示例

```
输入：grid = [[1,3,1],[1,5,1],[4,2,1]]
输出：7
解释：路径 1→3→1→1→1 的总和最小，为 7。
```

```
输入：grid = [[1,2,3],[4,5,6]]
输出：12
解释：路径 1→2→3→6，总和为 12。
```

约束：`1 <= m, n <= 200`

---

## 思路推导（DP 五步法）

### ① 定义状态

`dp[i][j]` = 从 `(0,0)` 到 `(i,j)` 的路径最小和。

### ② 写出转移方程

到达 `(i,j)` 只能从上方或左方来，取较小值加上当前格子的值：

$$
dp[i][j] = grid[i][j] + \min(dp[i-1][j],\; dp[i][j-1])
$$

### ③ 初始化

- `dp[0][0] = grid[0][0]`
- 第一行：`dp[0][j] = dp[0][j-1] + grid[0][j]`（只能从左边来）
- 第一列：`dp[i][0] = dp[i-1][0] + grid[i][0]`（只能从上面来）

### ④ 遍历顺序

`i` 从 0 到 `m-1`，`j` 从 0 到 `n-1`，按行优先遍历。

### ⑤ 返回值

`dp[m-1][n-1]`。

---

## Python 代码

### 写法一：二维 DP

```python
class Solution:
    def minPathSum(self, grid: list[list[int]]) -> int:
        m, n = len(grid), len(grid[0])
        dp = [[0] * n for _ in range(m)]
        dp[0][0] = grid[0][0]

        for j in range(1, n):
            dp[0][j] = dp[0][j-1] + grid[0][j]
        for i in range(1, m):
            dp[i][0] = dp[i-1][0] + grid[i][0]
        for i in range(1, m):
            for j in range(1, n):
                dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])

        return dp[m-1][n-1]
```

### 写法二：原地修改 grid

直接在 `grid` 上改，不需要额外空间。

```python
class Solution:
    def minPathSum(self, grid: list[list[int]]) -> int:
        m, n = len(grid), len(grid[0])
        for j in range(1, n):
            grid[0][j] += grid[0][j-1]
        for i in range(1, m):
            grid[i][0] += grid[i-1][0]
        for i in range(1, m):
            for j in range(1, n):
                grid[i][j] += min(grid[i-1][j], grid[i][j-1])
        return grid[m-1][n-1]
```

### 写法三：一维 DP

```python
class Solution:
    def minPathSum(self, grid: list[list[int]]) -> int:
        m, n = len(grid), len(grid[0])
        dp = [0] * n
        dp[0] = grid[0][0]
        for j in range(1, n):
            dp[j] = dp[j-1] + grid[0][j]
        for i in range(1, m):
            dp[0] += grid[i][0]
            for j in range(1, n):
                dp[j] = grid[i][j] + min(dp[j], dp[j-1])
        return dp[n-1]
```

---

## 复杂度分析

| 写法 | 时间 | 空间 |
|---|---|---|
| 二维 DP | O(mn) | O(mn) |
| 原地修改 | O(mn) | O(1) |
| 一维 DP | O(mn) | O(n) |

---

## 易错点

1. **第一行/列要累加**：不是直接取 `grid` 值，而是前面所有值的累加。
2. **别忘了加 `grid[i][j]` 本身**：转移是 `grid[i][j] + min(...)` 不是单纯的 `min(...)`。
3. **一维写法中 `dp[0]` 的更新**：每行开始前 `dp[0] += grid[i][0]`，这代表从上面下来的唯一路径。

---

## 知识点补充

### 和 62 不同路径的对比

| | 62 不同路径 | 64 最小路径和 |
|---|---|---|
| 格子值 | 无（全 1） | 各不相同 |
| 转移 | `dp[i-1][j] + dp[i][j-1]` | `grid[i][j] + min(...)` |
| 含义 | 计数（路径数） | 优化（路径和最小） |

两个题的框架完全一致，只是转移函数从"加法计数"变成了"取 min 优化"。

### 动态规划的三种目标类型

| 类型 | 转移动词 | 例子 |
|---|---|---|
| 计数 | `+` | [[62-不同路径]] |
| 最优化 | `max`/`min` | 64 最小路径和 |
| 判断 | `or` | [[139-单词拆分]] |

---

## 相关题目

- [[62-不同路径]]：相同框架，计数版。
- LeetCode 120 三角形最小路径和：三角形网格变形。
- LeetCode 174 地下城游戏：反向 DP（从终点推回起点）。
- [[5-最长回文子串]]：另一个二维 DP。
